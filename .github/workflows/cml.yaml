name: CML Model Comparison Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  train-compare-report:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Train all models
        run: |
          python train_experiments.py
      
      - name: Compare models and generate report
        run: |
          python compare_models.py
      
      - name: Create CML report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npm install -g @dvcorg/cml
          
          cat > report.md << 'EOF'
          # 🏆 Rapport de Comparaison de Modèles - Iris Classification

          Ce rapport compare automatiquement les performances de différents algorithmes de classification sur le dataset Iris.

          ## 🎯 Meilleur Modèle Sélectionné

          ```json
          EOF
          cat experiments/best_model.json >> report.md
          cat >> report.md << 'EOF'
          ```

          ## 📊 Comparaison des Accuracy

          ![Comparaison des Accuracy](./reports/accuracy_comparison.png)

          ## 📈 Comparaison de Toutes les Métriques

          ![Comparaison de Toutes les Métriques](./reports/all_metrics_comparison.png)

          ## 🔥 Heatmap des Performances

          ![Heatmap des Performances](./reports/performance_heatmap.png)

          ## 📋 Résultats Détaillés

          <details>
          <summary>Cliquez pour voir tous les résultats</summary>

          ```json
          EOF
          cat experiments/all_results.json >> report.md
          cat >> report.md << 'EOF'
          ```
          </details>
          EOF
          
          cml comment create report.md



        
